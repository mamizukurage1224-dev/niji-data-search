# にじチェッカー 運用ガイド

にじチェッカー（https://niji-checker.tool2026.workers.dev/ ）を動かし続けるための手引きです。
専門の知識がなくても、この手順どおりに進めれば大丈夫なように書いています。

## ふだんは何もしなくて大丈夫です

次のことは、すべて自動で動いています。

- 15分ごとに、Holodex から配信のデータを取ってサイトに反映します。
- 1日1回、新人のデビューや卒業がないかを確かめます。
- 6時間ごとに、データの更新が止まっていないかを確かめます。

何か対応が必要になったときだけ、GitHub の **Issue（知らせ）** が届きます。
Issue は [niji-data-search の Issues](https://github.com/mamizukurage1224-dev/niji-data-search/issues) に並び、メールでも届きます。

> メールが届かないときは、GitHub の右上のアイコン →「Settings」→「Notifications」で、メールの通知がオンになっているかを確かめてください。

## 知らせ（Issue）が届いたら

### 「ライバーマスターの見直しが必要なチャンネルがあります」

新人のデビューや卒業があったときに届きます。Issue の中に、そのチャンネルの名前とチャンネルID（UC から始まる文字）の表があります。

**新人を追加する**

1. [ライバー一覧を直す](https://github.com/mamizukurage1224-dev/niji-data-search/actions/workflows/master.yml) を開きます。
2. 右の「Run workflow」を押します。
3. 欄を埋めます。
   - すること：「新人を追加する」
   - チャンネルID：Issue の表からコピーして貼ります。
   - グループ：「にじさんじ」か「NIJISANJI EN」
   - 表示名：例「星導ショウ」
   - 読み：ひらがなで。例「ほしるべしょう」（NIJISANJI EN の人はカタカナで「ヴォックス・アクマ」のように）
   - 名前だけの読み：なくてもかまいません。例「しょう」
   - ライバーカラー：分かれば「#A58CDC」のように。分からなければ空のままで、あとから入れられます。
4. 緑の「Run workflow」を押します。
5. 1分ほどで緑のチェックが付けば完了です。15分以内にサイトのお気に入りの選択に出ます。

赤い×が付いたときは、それを押すと、何を直せばよいか（例：「チャンネルIDの形が違います」）が書いてあります。直してもう一度実行してください。

**卒業・活動終了にする**

同じ画面で、すること「卒業・活動終了にする」を選び、チャンネルIDだけ入れて実行します。お気に入りの選択から外れます。

**ライバー本人ではないチャンネル**（ユニット・公式・切り抜きなど）は、何もしなくてかまいません。

終わったら、Issue の下の「Close issue」で閉じてください。

> 追加が数日遅れても大丈夫です。配信のデータはにじさんじ全体を集めているので、追加した時点でデビューからの配信がさかのぼって出ます。

### 「データの更新が止まっています」

サイトのデータが3時間以上新しくなっていないときに届きます。いちばん多い原因は、**GitHub のトークン（合い言葉）の期限切れ** です。

1. 下の「年に1回：GitHub のトークンを作り直す」の手順で、トークンを作り直します。
2. 15分ほど待って、[radar の実行履歴](https://github.com/mamizukurage1224-dev/niji-data-search/actions/workflows/radar.yml) に新しい行が増えていれば直っています。Issue は自動で閉じます。
3. それでも直らないとき、または radar の行に赤い×が続いているときは、赤い×を押して出てくる画面を、Claude に見せて相談してください。

## 年に1回：GitHub のトークンを作り直す

15分ごとの更新は、Cloudflare から GitHub に「更新して」と頼む仕組みで動いています。そのときに使う合い言葉（トークン）には期限があります。**期限の日の少し前に**、次の手順で作り直してください（期限の日はカレンダーに入れておくと安心です）。

1. GitHub の右上のアイコン →「Settings」→ いちばん下の「Developer settings」→「Personal access tokens」→「Fine-grained tokens」を開きます。
2. 「niji-data-trigger」を押し、「Regenerate token」を押します。期限を選び直して、表示されたトークンをコピーします（一度しか表示されません）。
3. Cloudflare にログインし、「Workers & Pages」→「niji-data-trigger」→「設定」を開きます。
4. 「Runtime variables and secrets（変数とシークレット）」の「GITHUB_TOKEN」の右の編集（またはローテート）を押し、コピーしたトークンを貼って「デプロイ」を押します。
5. 15分ほど待って、[radar の実行履歴](https://github.com/mamizukurage1224-dev/niji-data-search/actions/workflows/radar.yml) に新しい行が増えれば完了です。

> トークンはどこにも貼ったり、人に見せたりしないでください（Claude とのやりとりにも貼らないでください）。

## 表示名・読み・色を直したいとき

[ライバー一覧を直す](https://github.com/mamizukurage1224-dev/niji-data-search/actions/workflows/master.yml) で、すること「表示名・読み・色を直す」を選び、チャンネルIDと、直したい欄だけを入れて実行します。

チャンネルIDは、[ライバー一覧（livers_master.csv）](https://github.com/mamizukurage1224-dev/niji-data-search/blob/main/livers_master.csv) の中を名前で探すと見つかります。

## お問い合わせが届いたら

お問い合わせは Google フォーム「にじチェッカーお問い合わせ」に届きます。

- **掲載をやめてほしいという依頼**（ご本人・関係者から）：「ライバー一覧を直す」で「卒業・活動終了にする」を選ぶと、その人をお気に入りに選べなくなります。対応の内容に迷ったら Claude に相談してください。
- **出演が載っていない、間違っている**：データは Holodex のものなので、Holodex 側の出演者の登録が直ると、サイトにも反映されます。
- **そのほか**：返信が必要なものは、フォームに書かれた連絡先に返信します。

## 見た目や機能を変えたいとき

Claude Code で `niji-oshikatsu-tools` を開き、やりたいことをそのまま伝えてください。これまでの経緯や決めごとは `CLAUDE.md` に書いてあるので、Claude はそれを読んで続きから進められます。

## お金について

いまの仕組みは、すべて無料の範囲で動いています。

- Cloudflare（サイトの置き場）：無料プラン。アクセスが増えても料金はかかりません。サイトの作り直し（ビルド）は月500回まで。
- GitHub（データの置き場と自動の更新）：公開リポジトリなので無料。
- Holodex（配信のデータ）：無料。広告や有料の機能を付けないことが条件です。

## 困ったとき

どこで困っているか（例：「運用ガイドの『データの更新が止まっています』の2で、行が増えない」）を Claude に伝えてください。
